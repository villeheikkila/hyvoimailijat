# Helsingin Yliopiston Voimailijat

```bash
pnpm install
pnpm refresh:staging
pnpm dev
```

Admin:

```text
http://localhost:4321/_emdash/admin
```

Local offline data:

```bash
pnpm refresh:local
pnpm dev:local
```

Checks:

```bash
pnpm typecheck
pnpm build
```

Deploy:

```bash
pnpm deploy
```
